<form method="post" action="{{ path('dev_delete', {'id': devoir.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ devoir.id) }}">
    <button class="btn">Delete</button>
</form>
